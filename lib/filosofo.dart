import 'package:flutter/material.dart';

class Filosofo extends StatelessWidget {
  const Filosofo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}