// Copie este arquivo para supabase_config.dart e preencha com as credenciais reais.
// Nunca commite o supabase_config.dart — ele está no .gitignore.
// Peça as credenciais ao responsável pelo projeto.

const supabaseUrl = 'https://<project-ref>.supabase.co';
const supabaseAnonKey = '<anon-public-key>'; // publishableKey
const supabaseBucket = 'assets';
